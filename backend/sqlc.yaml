version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/queries/"
    schema: "migrations/"
    gen:
      go:
        package: "dbgen"
        out: "internal/db/generated"
        emit_json_tags: true
        emit_interface: false
        emit_empty_slices: true
